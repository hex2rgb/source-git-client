services:
  filebrowser:
    image: filebrowser/filebrowser
    container_name: smba-fs-web
    restart: unless-stopped
    user: "1001:100"
    ports:
      - "58080:80"
    environment:
      TZ: Asia/Shanghai
    volumes:
      - /mnt/user/outerdir/workspace/remote-cc-workspace:/srv
      - /mnt/user/appdata/filebrowser/database:/database
      - /mnt/user/appdata/filebrowser/config:/config

  samba:
    image: dperson/samba
    container_name: smba-fs
    restart: unless-stopped
    stdin_open: true
    tty: true
    ports:
      - "55445:445/tcp"
    environment:
      TZ: Asia/Shanghai
      WORKGROUP: WORKGROUP
    volumes:
      - /mnt/user/outerdir/workspace/remote-cc-workspace:/mount
    command: >-
      -u "putong;111111;1001;users;100"
      -s "outerdir;/mount;no;no;no;putong;putong"