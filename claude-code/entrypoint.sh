#!/bin/bash
set -e

# ========================================
# 后台任务 — 在此填写需要后台运行的命令
# ========================================
# 每行一个，用 & 结尾，例如：
#   your-daemon-command &
#   your-other-command &

# ========================================

# 保活
exec tail -f /dev/null
